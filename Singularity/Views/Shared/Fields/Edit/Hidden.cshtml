@* Generator: MvcView *@

@{
}

@model IViewField

<input type="hidden"
       id="@Model.Meta.PropertyName"
       name="@Model.Meta.PropertyName"
       value="@((Model.PropertyData ?? "").ToString())" />