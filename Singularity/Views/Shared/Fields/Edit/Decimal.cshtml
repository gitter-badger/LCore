@* Generator: MvcView *@
@using LCore.Extensions;

@{
}

@model IViewField

@Html.TextBox(Model.Meta.PropertyName,
    Model.ModelData.GetProperty(Model.Meta.PropertyName),
    new { @class = "spinner-decimal" })