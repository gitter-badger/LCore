@* Generator: MvcView *@
@using Singularity.Extensions;

@{
    Template ModelData = (Template)Model.ModelData;
}

@model IViewField

<a href="@(Url.Controller<TemplateController>().Action(c=>c.PreviewPDF, ModelData.TemplateID))">
    <glyph>&#xe175;</glyph>
    View template PDF
</a>